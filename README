This is the bare-bones front-end for a "don't break the chain" calendar. 

To run this app:
- make sure you have npm installed
- open a terminal in the app directory and run `npm install`
- run `npm start`

Read more about the concept of "don't break the chain" here:
https://blog.doist.com/dont-break-the-chain/
